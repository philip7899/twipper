<div class="container hero_place " >
	<%= simple_form_for @tweet, defaults: {input_html: {class: 'form-inline'}} do |f| %>
		<div class="form-group">
	  		<%= f.input :tweet, placeholder: "What's on your mind?", input_html: {class: "form-control"}, label: false %>
	  	</div>
	  	<%= f.button :submit, class: 'btn btn-primary btn-block' %>
	<% end %>
</div>

<br>
